#include <iostream>
#include <sstream>


template <class TYPE> class vec3
{
	TYPE x, y, z;

	Zero()
	{
		if (x != nullptr && y != nullptr && z != nullptr)
		{
			TYPE x, y, z = 0;
		}
	}

	isZero(class vec3)
	{
		vec3.x
	}

	vec3::~vec3() //Destructor
	{

	}
};